<h1>All Events</h1>
<%= Event.all %>
	<% form_tag '/events/choose', :methods => :put do %>
		<ul>
			<% for event in @events %>
				<li>
					<%= check_box_tag "event_ids[]", event.id %>
					<%= event.event %>
				</li>
			<% end %>
		</ul>
		<%= submit_tag "Update with choose" %>
	<% end %>
